# 010 — CRM (HubSpot / Salesforce)

> Estado: `pendiente` | Categoría: `integraciones` | Prioridad: `media`

## Qué es

Integración con sistemas CRM como HubSpot o Salesforce para sincronizar contactos, gestionar deals/oportunidades, registrar actividades y consultar datos de clientes desde el agente IA. Se implementa como un conjunto de herramientas MCP más un servicio de sincronización.

## Por qué es necesario para B2B

El CRM es el sistema central de cualquier equipo de ventas y atención al cliente. Integrar Clawmint con un CRM permite a los agentes de IA consultar información de clientes en tiempo real, registrar interacciones automáticamente, actualizar el pipeline de ventas y generar reportes, todo desde el terminal o un chat. Elimina la doble captura de datos y mantiene el CRM actualizado sin esfuerzo manual.

## Qué existe hoy

- Infraestructura MCP con herramientas en `server/mcp/tools/`.
- Sistema de contactos propio en SQLite (`contact_add`, `contact_list`, etc.).
- Agentes IA que ya pueden invocar herramientas MCP desde cualquier canal.
- Patrón OAuth2 que se implementará en `005` y `006` y se puede reutilizar.

## Alcance mínimo (MVP)

- Implementar autenticación OAuth2 para HubSpot (API v3) como CRM inicial.
- Crear herramienta MCP `crm_contact_search` para buscar contactos por nombre/email.
- Crear herramienta MCP `crm_contact_create` para crear un contacto nuevo.
- Crear herramienta MCP `crm_deal_list` para listar oportunidades/deals.
- Crear herramienta MCP `crm_deal_create` para crear un deal asociado a un contacto.
- Crear herramienta MCP `crm_activity_log` para registrar una nota o actividad en un contacto.
- Servicio de sincronización básica: mapear contactos internos de Clawmint con contactos del CRM.
- Interfaz abstracta (`BaseCRM.js`) para soportar múltiples proveedores CRM a futuro.

## Dependencias

- Requiere: cuenta de HubSpot (plan gratuito sirve para desarrollo) o Salesforce Developer.
- Requiere: OAuth2 implementado (se beneficia de `005-google-calendar`).
- Se integra con: sistema de contactos existente en Clawmint.
- Habilita: automatizaciones de ventas, reportes de pipeline, registro automático de interacciones.
- Relacionado con: `008-whatsapp-business.me` y `009-email-smtp-imap.me` (canales donde ocurren las interacciones que se registran en el CRM).

## Archivos probables

- `server/mcp/tools/crm-contact-search.js`
- `server/mcp/tools/crm-contact-create.js`
- `server/mcp/tools/crm-deal-list.js`
- `server/mcp/tools/crm-deal-create.js`
- `server/mcp/tools/crm-activity-log.js`
- `server/services/crm/BaseCRM.js` — interfaz abstracta para proveedores CRM
- `server/services/crm/HubSpotCRM.js` — implementación para HubSpot
- `server/services/crm/crm-sync.js` — sincronización de contactos
